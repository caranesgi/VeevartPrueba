 @IsTest
public class testJugada {
	@IsTest
    public static Void generalTest(){
        Integer numJugadores = 6;
        Integer won = 0;
        System.assertEquals(0, won);
        won = jugada.init(6, 4, 4);
        System.assertEquals(1, won);
    }
}